apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vc-authn-chart.fullname" . }}-test-connection"
  labels:
    {{- include "vc-authn-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vc-authn-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
